
需求会话历史记录：

1.
index页面，
右上角设置按钮，层级管理弹窗
添加确认之后，在页面卡片层后面新增一层，按卡片层级对应主题色，
编辑确认后，页面卡片的层级名称保存改变，
删除弹窗，使用提示弹窗“确认删除？”，确认后，删除页面对应卡片层级

页面里的单个卡片：
添加按钮，弹窗里的教师参考，填写了内容则显示卡片里的教师参考按钮，编辑操作同理。

新增的卡片:
卡片的右上角的展开收起按钮，默认展开状态，鼠标移上去显示[收起]提示;
新增的卡片,去掉鼠标hover[展开收起]按钮显示3个操作按钮，改成hover卡片显示隐藏
卡片里的操作按钮（编辑，关联子级，删除卡片）图标，使用svg生成或其他方式，达到的效果，显示操作按钮时，不要影响卡片里的其他元素的空间就行。

hover卡片时，且卡片处于展开状态，
若卡片是最高层级卡片，该卡片的所有下级，下级的下级等等依次递归查找出来，将它们的连线变粗；非下级的卡片变灰，连线隐藏，
若卡片是最低层级卡片，则对所有上级做相同处理；
若卡片处于中间层级，则找出所有上下级，做相同出处理

卡牌组：
收起卡片时，卡片按所在层级位置从上到下排序堆叠，
若是第一层级，则第一层级卡片与堆叠的第一个卡片连线，其余连线隐藏或移除，展开时需要恢复；
若不是第一层级，则为卡片组第一个卡片连接下一个卡片组的第一个卡片，其余做相同处理。

2.

鼠标拖拽连线，大概在连接点边缘的位置，连接点和操作按钮一直在“闪现”，出现隐藏，非常快速，导致不好的观感，需要修复。
新增层级，里面的[+添加]按钮宽度过小，需要跟非新增的按钮一致；新增卡片，当填写了教师参考内容，需要在新增的卡片上显示教师参考按钮。

连线和卡片堆叠效果不对，
举例说明，当卡片关系为：
{startId'a1', endId'b1'}
{startId: 'a1', endId: 'b1'}
{startId: 'c1', endId: 'd1'}
{startId: 'b1', endId: 'c2'}
{startId: 'b1', endId: 'c3'}
{startId: 'a1', endId: 'b2'}
{startId: 'c1', endId: 'd2'}
{startId: 'a2', endId: 'b2'}
{startId: 'a2', endId: 'b3'}
{startId: 'c4', endId: 'd3'}
{startId: 'c4', endId: 'd2'}
{startId: 'b1', endId: 'c1'}
{startId: 'a1', endId: 'b3'}
{startId: 'a2', endId: 'b4'}
{startId: 'b4', endId: 'c3'}
{startId: 'b4', endId: 'c4'}
{startId: 'b4', endId: 'c2'}


展开时，鼠标移到A1卡片上（最高层级卡片），则它的所有下级为B1,B2,B3,C1,C2,C3,D1,D2，所以除了这些卡片其他卡片都要变灰，线条移除或隐藏。
收起A1，则第二层级堆叠顺序B1b2b3,第三层及堆叠顺序为C1,C2,C3，依次D1,D2；A1与B2，B3连线隐藏或移除，二层堆叠B1连接三层堆叠C1，B1与C2,C3的连线移除或隐藏，三层同理。


3.
展开时，最上层和最底层的hover正确了，中间层的卡片hover不敌。
当hover中间层B1时，上层A1,下级C1C2C3,D1,D2，连线加粗，其它卡片变灰。
收起A1时，堆叠的B1应该连接堆叠的C1，而不是非堆叠的C1。其他堆叠同理。

当hover非堆叠的卡片时，比如收起A1，对A2做hover，A1及其以下的堆叠卡组都变灰。hover其他卡片同理

4.隐藏连线时，鼠标hover隐藏状态的连线路径，会显示小红叉，修复这个问题。
最后一层的起点，右上角的连线数量图标，关联子级按钮隐藏。
收起A1时，会堆叠所有关联下级，此时，非堆叠卡片都要变灰，连线隐藏。
[收起 A1 后 hover A2（或非堆叠B2以及其他非堆叠卡片），A1 下游堆叠组变灰 + 连线隐藏]这个问题没有成功。
[hover 非堆叠 C1 时同理，并隐藏 B1->C1，不加粗收起链路]这个问题没有成功。
(1) hover非堆叠卡片(除了A1)，A1及其堆叠下游全部变灰，连线隐藏；
（2）hover A2时,执行（1）效果，且A2的所有下游连线加粗，示例包括：A2->B2B3B4,B2->C1,B4->C2C3C4,C1->D1D2,C4->D2D3，注意这些都是非堆叠卡片；
hover B2时，除了A2->B3B4,其余同（2），依次hover其他卡片同理。


鼠标放在连接点边缘的位置，大概连线和连接点交叉的位置，
卡片上的操作按钮，连线上的小红叉，一直在“闪现”，出现隐藏，非常快速，导致不好的观感，需要修复。

鼠标放在卡片上时的连线，z-index比连接点高，观感不好，需要修复。