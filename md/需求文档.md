[一]
基于已完成的功能，现需对index、questionMapDetail和goLink三个页面中的卡片及连线实现统一的技术规范。具体要求如下：

1. 页面卡片层级实现规范：
   - 统一三个页面中卡片层级的HTML元素属性定义，确保属性命名、数据结构及DOM层级关系保持一致
   - 规范卡片的CSS样式实现，包括但不限于卡片尺寸、边距、阴影效果及响应式布局规则
   - 统一卡片的布局结构，确保在不同页面中保持视觉一致性和交互统一性

2. 卡片样式规范：
   - 标题区域：统一字体大小、粗细、颜色及文本溢出处理方式
   - 详情内容：规范文本展示格式、行高、间距及最大显示行数
   - 编辑功能：统一编辑按钮的位置、样式及交互反馈效果
   - 主题颜色：建立统一的主题色体系，确保不同类型卡片的颜色标识一致
   - 连接数量：规范连接数量的展示位置、样式及数字格式

3. 卡片连线样式规范：
   - 线条渐变颜色：定义统一的渐变色彩方案及方向规则
   - 选中状态：规范选中时线条及选中框的颜色、粗细及动画效果
   - 线条样式：统一线条的宽度、类型（实线/虚线）及端点样式

4. JavaScript实现规范：
   - 完全依照index页面的卡片实现方式，确保核心逻辑、数据处理及事件绑定保持一致
   - 对于类似功能，需参考index页面的实现模式，保持代码风格和设计模式的统一性
   - 确保三个页面中卡片及连线的创建、更新、删除等操作逻辑一致

所有实现必须保证在不同页面间的视觉一致性、交互统一性及代码可维护性，同时确保与现有功能的兼容性。



[二]
针对targetMapTeacher页面，需进行以下功能优化与样式修复，确保界面交互友好、视觉呈现清晰：

1. 课程能力与知识点关联关系区块优化：
   - 页面加载完成后，能力列表区域默认自动选中第一个能力项
   - 右侧SVG关系图（知识点关联图）需进行以下优化：
     a) 调整箭头样式，使其视觉上更美观且指向明确
     b) 实现文字在图形内部居中显示的布局
     c) 当文字内容过长时，以图形圆心为中心，使文字自然延伸至图形外部显示
     d) 合理调整图形间间距，确保图形不相互重叠；当文字过长时，保持与文字的安全距离，避免覆盖其他图形元素

2. 右上角关联知识点弹窗样式修复：
   - 修正弹窗现有样式混乱问题，确保布局合理、元素对齐
   - 实现父级勾选框在展开子级列表时保持可见状态
   - 确保父级与子级勾选框为独立控制，勾选状态互不影响
   - 添加hover交互效果：当鼠标悬停在父级或子级项目上时，显示统一的背景色高亮效果

3. 一致性要求：
   - 关联知识点弹窗的整体样式、交互逻辑、勾选行为需与"内容管理"模块中"新建能力"或"编辑能力"功能的关联知识点弹窗保持完全一致


[三]
questionMapDetail页面：
1. 卡片连接数量显示优化：除第一层级外，修正中间层级连接数量的显示错误，确保数据准确性；最低层级的连接数量不显示。实现方式需完全参考index页面的处理逻辑。

2. 连线颜色优化：实现连线颜色跟随卡片主题色的渐变效果；解决因渐变导致相邻卡片水平直线连接时连线不可见的问题。修复方案需参考index页面的对应实现。

3. 连接点优化：使用SVG技术绘制连接点；确保连接点的z-index值高于连线，避免连线遮挡连接点。实现方式需参考index页面的处理方式。

4. 卡片编辑按钮优化：使用SVG绘制编辑按钮图标；确保图标样式与index页面保持完全一致。

goLink页面：
1. 第一层卡片连接数量显示调整：将第一层卡片的连接数量默认显示在右侧连接点处；移除右上角的连接数量显示元素。

2. 连接点优化：使用SVG技术绘制连接点；确保连接点的z-index值高于连线，避免连线遮挡连接点。实现方式需参考index页面的处理方式。

3. 连线颜色优化：实现连线颜色跟随卡片主题色的渐变效果；解决因渐变导致相邻卡片水平直线连接时连线不可见的问题。修复方案需参考index页面的对应实现。

通用要求：
针对上述所有卡片相关问题，必须统一采用index页面的处理方式；对于各页面中相同的功能模块和实现逻辑，需进行统一抽象和复用，避免重复开发。






# index页面
1. 移除堆叠卡片的点击跳转questionMapDetail页面功能，确保堆叠卡片区域不响应任何点击导航事件。
2. 保留堆叠组的连接圆点，调整连线位置使其准确居中于堆叠卡组；优化连线布局，解决连线"往下掉"的视觉偏移问题。
3. 修复堆叠A2元素下游缺失连线的问题，确保所有必要的下游连接关系正确显示。
4. 实现卡片终点的拖拽连线功能：允许用户从卡片终点拖拽线条，并精确连接至相邻上级卡片的起点位置，确保连接交互流畅自然。

# questionMapDetail页面
1. 使用SVG格式重新绘制编辑按钮，确保图标清晰度与界面风格统一。
2. 编辑弹窗中的"描述"和"教师参考"字段需实现为富文本编辑框，严格参考index页面卡片编辑弹窗的交互逻辑、样式设计和功能特性，保持用户体验一致性。
3. 修复因渐变设置导致连线长度为0而不显示的bug，参考index页面卡片连线的渐变实现方案，确保连线正常显示。
4. 实现卡片连线的渐变色功能：连线颜色应从一个层级卡片的主题色平滑过渡到另一个层级卡片的主题色。
5. 修复拖拽整体卡片时连接点与卡片之间的视觉延迟偏移问题，确保连接点与卡片保持同步移动，提升交互流畅度。

# goLink页面
1. 实现卡片连线的渐变色功能：连线颜色应从一个层级卡片的主题色平滑过渡到另一个层级卡片的主题色，确保视觉效果与questionMapDetail页面保持一致。


# targetMapTeacher页面
针对以下UI和交互问题进行修复和优化：

1. 课程能力与知识点关联关系区块：
   - 修复显隐设置按钮的点击区域问题，确保点击按钮文字以外的按钮区域也能正常触发弹窗，实现整个按钮区域的可点击性

2. 显隐设置弹窗优化：
   - 调整布局使"显隐设置"、"能力名称"、"开放班级"等列标题左对齐
   - 优化边框样式，仅保留弹窗最外框的左边框(border-left)，或完全移除所有左边框

3. 关联知识点弹窗优化：
   - 修复与显隐设置弹窗相同的点击区域问题，确保整个按钮可点击
   - 调整文字区块使其左对齐
   - 统一边框样式，仅保留弹窗最外框的左边框(border-left)，或完全移除所有左边框

4. 关联知识点弹窗功能优化：
   - 确保父级勾选框在展开子级时始终可见
   - 实现父级与子级勾选框的独立选择功能，两者的勾选状态互不影响
   - 添加hover效果，当鼠标移至父级或子级项目时，应显示背景色变化

5. SVG画布图形优化：
   - 移除图形中的"能力"等级展示
   - 仅保留并正确显示知识点层级中的父子关系图形